# 자료구조

list에 대해 공부하였는데 머리로는 이해하였는데,
막상 실제로 내가 처음부터 끝까지 구현하려고 하니 조금씩 코드를 볼 수 밖에 없었다.
아직 이해가 완전히 되지는 않았나 보다...

배열로 구현한 리스트(함수 버전)에 대해 조금 더 공부해야할 것 같다

함수 버전이 아닌 클래스 버전은 사실 필요한 클래스를 선언하고,
전역변수로 선언된 데이터-> 클래스의 멤버 변수
일반함수로 구현된 연산 -> 클래스의 멤버 함수
매개변수로 .self만 추가해주면 되어서 함수 버전만 잘 안다면 클래스버전은 무리 없이 잘 할 수 있다고 생각한다.

#논리회로설계
Boolean logic 에 대해 배우고

Sop ( sum of product)에 대해 공부하였다.
minterm 과 maxterm에 대해서는 더 공부할 예정이다
이해하지 못한 느낌이다...

그 외에 truth table이나 and or xor 연산에 대해서는 확실히 알게되었다.
